version: 2

models:
  - name: foo
    config:
      group: yay
    columns:
      - name: id
        tests:
          - unique:
              config: 
                severity: warn
  - name: bar
    config:
      group: yay
    columns:
      - name: id
        tests:
          - unique